<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/newAssignmentTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@null"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:hint="Title"/>

    <EditText
        android:id="@+id/newAssignmentDueDate"
        android:layout_width="match_parent"
        android:layout_below="@+id/newAssignmentTitle"
        android:layout_height="wrap_content"
        android:background="@null"
        android:hint="Due date (MM/DD/YYYY)"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium" />
    <EditText
        android:id="@+id/newAssignmentClass"
        android:layout_width="match_parent"
        android:layout_below="@+id/newAssignmentDueDate"
        android:layout_height="wrap_content"
        android:background="@null"
        android:hint="Class Name"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium" />

    <Button
        android:id="@+id/newAssignmentButtonCreate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/newAssignmentClass"
        android:layout_alignParentEnd="true"
        android:textSize="16sp"
        android:text="Save"
        android:textAllCaps="false"
        android:background="@android:color/transparent"
        android:textColor="@android:color/darker_gray"/>


</RelativeLayout>